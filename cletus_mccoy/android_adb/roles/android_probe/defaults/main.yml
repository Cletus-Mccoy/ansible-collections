---
# Default values for android_probe role
local_ip: "127.0.0.1"
adb_port: "5555"

# Include system packages in the app inventory (false = user-installed only).
probe_include_system_apps: false
